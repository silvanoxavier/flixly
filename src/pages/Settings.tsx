"use client";

import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const Settings = () => (
  <Card>
    <CardHeader>
      <CardTitle>Configurações</CardTitle>
    </CardHeader>
    <CardContent>
      <p>Configurações gerais em breve com Convex.</p>
    </CardContent>
  </Card>
);

export default Settings;